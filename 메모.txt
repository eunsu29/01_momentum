html
태그	설명	예시
h1~h6	html 기본 크기	<h1>내용</h1>
hr	수평줄이 그어짐	<hr>
br	한줄 공백	<br>
p	본문글자	<p>내용</p>
a	웹페이지 연결	<a href="주소">표시텍스트</a>
	페이지 내 이동	<a href="#지정">표시텍스트</a>
		<@ id="지정"> <-이동
ol	순서 있는 목록	ol>li*숫자
ul	순서 없는 목록	
li	목록 내에 항목	ol, ul 코드 안에 위치
tr	표 줄수↓	tr,th,td <table>코드 안에 위치
th	표 내용(굵게)→	<tr><th></th></tr>
td	표 내용(일반)→	<tr><td></td></tr>
thead		
tbody		
tfoot		
col	세로 설정	

react native 안드로이드 ios 앱
electron
socket.io 실시간 채팅
ml5.js 머신러닝

replit.com 프로그램없이 코딩


integer 정수
float 실수
string 문자열
console.log(); 콘솔에 표시

variable 변수
const a = 5; // 값 변경불가
let // 값 변경가능
boolean // true or false
null // 비어있음(자연적으로 발생 X)
undefinde // 정의되지않음

array // 리스트
array.push() // 맨 뒤에 추가

object
const player = {
	name: "abc",
	points: 10
}
player.name -> abc
player.age = 20 // 마지막에 추가

function sayHello(name){
	console.log("Hello " + name);
}
sayHello("Eunsu");

const player = {
	name: "abc",
	sayHello: function(name) {
		console.log("Hello " + name)
	}
}

player.sayHello("def"); -> Hello def

prompt() // 입력팝업, css 적용불가

typeof age // 변수 타입확인

parseInt() // 정수로 변경

isNaN // 숫자확인, NaN -> true | number -> false

if(조건) {
	만족
} else {
	불만족
}

&& // and
|| // or


document // javascrpit html 연결되어있음
document.title = "Hi" // title 변경가능

document.getElementById("title") // id로 태그 호출
































